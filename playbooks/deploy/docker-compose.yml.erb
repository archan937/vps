version: "3"

services:

  <%- services.each do |service| %>
  <%= service.name %>:
    image: <%= service.image %>
    container_name: <%= service.name %>

  <%- end %>
  <%- images.each do |image| %>
  <%= image.name %>:
    image: <%= image.name %>
    container_name: <%= image.name %>

  <%- end %>
volumes:
